

Для машин типа Local - размер диска ограничен выбранной конфигурацией. Для Volume-backed машин можно выбрать нужный обьем тома и в случае чего его увеличить.
Добавить что можно том создать сначала нужного размера и потом с ним создать виртуалку.


🔹 Local (локальный диск)

Хранится на SSD/NVMe хоста.

Быстрее, выше IOPS.

При удалении ВМ — данные теряются (сохраняются только в снимке).

Поддерживает: snapshot, backup, rescue, rebuild.

При Shelve создаёт снимок -shelved, при Unshelve может не запуститься, если нет свободных GPU/CPU.

🔹 Volume-backed (сетевой том)

Хранится на Cinder-хранилище (HDD/SSD).

Том живёт отдельно от ВМ.

При удалении ВМ данные сохраняются.

Нельзя делать snapshot, backup, rescue, rebuild.

Удобно для долгосрочного хранения и клонирования.

🔄 Операции и переходы

Из Local → Volume-backed:

Выключить ВМ.

Сделать снимок.

Создать новый сервер → тип Volume-backed → источник «Новый том из снимка».

Из Volume-backed → Local:

Создать образ из Тома.

Создать новую ВМ типа Local → источник «Из образа».